<local:BasePage x:TypeArguments="local1:MainViewModel"  x:Class="Game.Presentation.Pages.Main"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Game.Presentation.Pages"
      xmlns:local1="clr-namespace:Game.Presentation"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Main">

    <StackPanel>
        <Grid Height="600">
            <StackPanel Margin="250 80 250 110">
                <Button Style="{StaticResource NormalButton}" x:Name="btnTheme" Command="{Binding ThemeCommand}" Content="THEO CHỦ ĐỀ" Margin="0 0 0 40" Height="65" FontSize="18" FontFamily="Arial"/>
                <Button Style="{StaticResource NormalButton}" x:Name="btnNoThemes" Command="{Binding NoThemeCommand}" Content="KHÔNG THEO CHỦ ĐỀ" Margin="0 0 0 40" Height="65" FontSize="18" FontFamily="Arial"/>
                <Button Style="{StaticResource NormalButton}" Content="DANH SÁCH TỪ VỰNG" Margin="0 0 0 40" Height="65" FontSize="18" FontFamily="Arial"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0 0 2 0">
                        <Button Style="{StaticResource NormalButton}" Content="LỊCH SỬ" Height="30" Margin="0 0 0 5" FontSize="14" FontFamily="Arial"/>
                        <Button Style="{StaticResource NormalButton}" Content="CÀI ĐẶT" Height="30" FontSize="14" FontFamily="Arial"/>
                    </StackPanel>
                    <Button Style="{StaticResource NormalButton}" x:Name="btnExit" Content="THOÁT" Grid.Column="1"  Margin="2 0 0 0" Height="65" FontSize="18" Click="btnExit_Click" FontFamily="Arial"/>
                </Grid>
            </StackPanel>
        </Grid>
    </StackPanel>
</local:BasePage>
