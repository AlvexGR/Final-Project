<local:BasePage x:TypeArguments="viewModel:LoginViewModel" x:Class="Game.Presentation.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Game.Presentation.Pages"
      xmlns:viewModel="clr-namespace:Game.Presentation"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Login">
    <StackPanel>
        <Grid Height="570">
            <StackPanel Margin="230 80">
                <StackPanel Margin="0 30 0 0">
                    <TextBlock Text="Tên đăng nhập" Padding="5 5" FontSize="20"/>
                    <TextBox Padding="5 5" FontSize="20"/>
                </StackPanel>
                <StackPanel Margin="0 10 0 0">
                    <TextBlock Text="Mật khẩu" Padding="5 5" FontSize="20"/>
                    <TextBox Padding="5 5" FontSize="20"/>
                </StackPanel>
                <Button x:Name="btnLogin" Style="{StaticResource NormalButton}" Margin="70 20 70 0" Height="50" Content="ĐĂNG NHẬP"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</local:BasePage>
